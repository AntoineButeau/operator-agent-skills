# Local validation

Run the repository checks from the project root:

```bash
python3 scripts/validate.py
```

The validator checks pack and skill counts, required pack files, required skill sections, manifest path consistency, maturity/Ghost metadata, local examples, draft markers, and absence of `.github/workflows`.
