# Examples

Each pack has one realistic local-use example under `examples/by-pack/`. The examples show the minimum context an agent needs, a prompt shape, expected output structure, and the human review gate to preserve before acting externally.

These examples are intentionally concise. Adapt them to the current operating context rather than treating them as scripts.
